{
  "name": "aiops-aws",
  "version": "1.0.0",
  "description": "Intelligent Cloud Automation AIOps MVP",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test $(find dist -name '*.test.js' -print)"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.1111.0",
    "@aws-sdk/client-ec2": "^3.1111.0",
    "@aws-sdk/client-sns": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.1112.0",
    "@google/genai": "^2.17.1",
    "esbuild": "^0.25.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
